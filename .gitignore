# dotagent MCP index — derived, never committed
*.db

# Rust
/target/
**/*.rs.bk
Cargo.lock.orig

# makepkg build outputs
/pkg/
/src/
*.pkg.tar.*

# local debug captures
door.log
*.dump

# editor / OS
*.swp
.DS_Store

# local scratch / temp scripts (survives reboot, unlike /tmp)
/scratch/
.agent/
CLAUDE.md
vendor/
